<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
      </div>
    </div>
     <% if (username){ %>
        <form class="form-inline" method="POST" action="/logout">
          <div class="form-group-mb-2">
            <h5> Logged in as: <%=username %>
            <button type="submit" class="btn btn-outline-dark" type="text" name="Logged in as ${username}" style="margin-left: 1em;">Logout</button>
          </h5></div>
        </form>
        <% } else { %>  
          <form class="form-inline" action="/login" method="POST" style="margin-left: 0em;">
            <div class="form-group mb-2">
              <input class="form-control mr-sm-2" type="text" name="username" placeholder="Username" style="width: 200px; margin-left: 1em">
              <button type="submit" class="btn btn-primary">Login</button>
            </div>
          </form>
          <% } %>
        </div>
          </div>
        </form>

  
  </nav>
</header>